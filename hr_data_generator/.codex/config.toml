# Project-scoped Codex subagent defaults.
# Custom files in .codex/agents/ override these values when selected.

[agents]
enabled = true
max_concurrent_threads_per_session = 4
default_subagent_model = "gpt-5.6-luna"
default_subagent_reasoning_effort = "medium"
